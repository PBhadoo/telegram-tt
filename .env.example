NODE_ENV=development

TELEGRAM_API_ID=
TELEGRAM_API_HASH=

BASE_URL=https://web.telegram.org/a/

# Optional: Set a default proxy domain (auto-enables proxy for all users)
# Users can override this on the login page. Leave empty to disable by default.
PROXY_URL=
